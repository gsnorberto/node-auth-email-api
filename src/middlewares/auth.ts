//AUTH - JWT (Jason Web Token)

import { Request, Response, NextFunction } from "express"
import { User } from "../models/User";

export const Auth = {
   private: async (req: Request, res: Response, next: NextFunction) => {
      
   }
}